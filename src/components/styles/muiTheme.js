import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ef9974',
      main: '#f08353',
      dark: '#e26d3b',
      contrastText: '#fff'
    },
    secondary: {
      light: '#ff7961',
      main: '#035f6f',
      dark: '#f08353',
      contrastText: '#000'
    }
  }
});